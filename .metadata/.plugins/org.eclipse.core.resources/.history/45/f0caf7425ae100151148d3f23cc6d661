package tast1_1Bank;

public class BankPoduct {

	
	
	
	
	
	public BankPoduct() {
		// TODO Auto-generated constructor stub
	}
	
	
	private String generateIban() {
		for (int index = 0; index < (THE_LENGTH_OF_IBAN - iban.length()); index++) {
			this.iban += (int) (Math.random() * 10);
		}
		return this.iban;
	}

}
